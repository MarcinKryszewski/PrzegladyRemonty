<UserControl x:Class="PrzegladyRemonty.Features.Transporters.TransportersDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PrzegladyRemonty.Features.Transporters"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock FontSize="40"
                Foreground="Wheat"
                Text="Details" />
            <Button Content="Main"
                Height="40"
                Command="{Binding NavigateMainCommand}"
                Style="{StaticResource NavButton}" />
            <Button Height="40"
                Command="{Binding NavigateEditCommand}"
                Content="Edit"
                Style="{StaticResource NavButton}" />
            <Button Height="40"
                Command="{Binding NavigateAddCommand}"
                Content="Add"
                Style="{StaticResource NavButton}" />
        </StackPanel>
    </Grid>
</UserControl>
